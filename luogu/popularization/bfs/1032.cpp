#include<iostream>
#include<queue>
using namespace std;
const int maxn = 25;
char a[maxn], b[maxn];
char A[6][maxn], B[6][maxn];
int n = 1;

void bfs(){
    queue<char[21]>q;
    q.push(a);
    
}
int main()
{
    cin >> a >> b;
    while(cin >> A[n] >> B[n]) 
        ++n;
    --n;

}